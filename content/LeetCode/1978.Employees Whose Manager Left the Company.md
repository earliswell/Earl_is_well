---
Number: 1978.Employees Whose Manager Left the Company
draft: 
tags:
  - 코테
  - 데이터베이스
  - 알고리즘
website: https://leetcode.com/problems/employees-whose-manager-left-the-company/?envType=study-plan-v2&envId=top-sql-50
---
## Problem

Find the IDs of the employees whose salary is strictly less than `$30000` and whose manager left the company. When a manager leaves the company, their information is deleted from the `Employees` table, but the reports still have their `manager_id` set to the manager that left.

Return the result table ordered by `employee_id`.

The result format is in the following example.

## My Answer.
```python
import pandas as pd

def find_employees(employees: pd.DataFrame) -> pd.DataFrame:
	df = employees.loc[(employees['salary'] < 30000) & (~employees['manager_id'].isnull()), ["manager_id", "employee_id"]]
	
	return df.loc[~df['manager_id'].isin(employees["employee_id"]), ['employee_id']].sort_values(by='employee_id')
```

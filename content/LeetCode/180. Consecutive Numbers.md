---
Number: 180. Consecutive Numbers
draft: 
tags:
  - 코테
  - 데이터베이스
  - 알고리즘
website: https://leetcode.com/problems/consecutive-numbers/description/?envType=study-plan-v2&envId=top-sql-50
---
## Problem

Find all numbers that appear at least three times consecutively.

Return the result table in **any order**.

The result format is in the following example.

## My Answer.
```python
import pandas as pd

def consecutive_numbers(logs: pd.DataFrame) -> pd.DataFrame:
	
	logs['num_1'] = logs['num'].shift(1)
	logs['num_2'] = logs['num_1'].shift(1)
	
	return logs[(logs['num'] == logs['num_1']) & (logs['num'] == logs['num_2'])]['num'].to_frame().rename(columns={'num':'ConsecutiveNums'}).drop_duplicates()
```

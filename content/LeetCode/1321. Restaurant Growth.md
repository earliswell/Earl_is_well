---
Number: 1321. Restaurant Growth
draft: 
tags:
  - 코테
  - 데이터베이스
  - 알고리즘
website: https://leetcode.com/problems/restaurant-growth/description/?envType=study-plan-v2&envId=top-sql-50
---
## Problem

You are the restaurant owner and you want to analyze a possible expansion (there will be at least one customer every day).

Compute the moving average of how much the customer paid in a seven days window (i.e., current day + 6 days before). `average_amount` should be **rounded to two decimal places**.

Return the result table ordered by `visited_on` **in ascending order**.

The result format is in the following example.

## My Answer.
```python
import pandas as pd

def restaurant_growth(customer: pd.DataFrame) -> pd.DataFrame:
	
	customer.sort_values('visited_on', inplace=True)
	df = customer.groupby('visited_on', as_index=False).agg({"amount":"sum"})
	
	df['rolling_sum'] = df['amount'].rolling(window=7).sum()
	
	df.dropna(subset='rolling_sum', inplace=True)
	df.drop('amount', axis=1, inplace=True)
	df.rename(columns={'rolling_sum': 'amount'}, inplace=True)
	
	df['amount'] = df['amount'].astype(int)
	df['average_amount'] = np.round((df['amount'] / 7), 2)

return df
```

- [df.rolling, Documents](https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.rolling.html)
- [참고 2, wikidocs](https://wikidocs.net/152788)
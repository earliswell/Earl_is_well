---
Number: 1070. Product Sales Analysis 3
draft: 
tags:
  - 코테
  - 데이터베이스
  - 알고리즘
website: https://leetcode.com/problems/product-sales-analysis-iii/description/?envType=study-plan-v2&envId=top-sql-50
---
## Problem

Write a solution to select the **product id**, **year**, **quantity**, and **price** for the **first year** of every product sold.

Return the resulting table in **any order**.

The result format is in the following example.

## My Answer.
```python
import pandas as pd 
def sales_analysis(sales: pd.DataFrame, product: pd.DataFrame) -> pd.DataFrame: 
	sales['rnk'] = sales.groupby(['product_id'])['year' ].rank(method='dense') 
	return sales.loc[sales.rnk == 1 ].merge(product).iloc[:,[1,2,3,4] ].rename(columns = {'year': 'first_year'})
```


---
Number: 596. Classes More than 5 Students
draft: 
tags:
  - 코테
  - 데이터베이스
  - 알고리즘
website: https://leetcode.com/problems/classes-more-than-5-students/description/?envType=study-plan-v2&envId=top-sql-50
---
## Problem

Write a solution to find all the classes that have **at least five students**.

Return the result table in **any order**.

The result format is in the following example.

## My Answer.
```python
import pandas as pd

def find_classes(courses: pd.DataFrame) -> pd.DataFrame:
	
	new_df = courses.groupby(['class']).count().reset_index()
	# courses['class'].value_counts().reset_index() 를 활용해도 됨
	return new_df[new_df['student'] >= 5][['class']]
```
- value_counts()에 reset_index를 하면 dataframe으로 변경되네 !

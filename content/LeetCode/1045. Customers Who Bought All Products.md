---
Number: 1045. Customers Who Bought All Products
draft: 
tags:
  - 코테
  - 데이터베이스
  - 알고리즘
website: https://leetcode.com/problems/customers-who-bought-all-products/description/?envType=study-plan-v2&envId=top-sql-50
---
## Problem

Write a solution to report the customer ids from the `Customer` table that bought all the products in the `Product` table.

Return the result table in **any order**.

The result format is in the following example.

## My Answer.
```python
import pandas as pd

def find_customers(customer: pd.DataFrame, product: pd.DataFrame) -> pd.DataFrame:
	
	customer = customer[customer['product_key'].isin(product['product_key'].tolist())]
	df = customer.groupby(['customer_id'])['product_key'].nunique().reset_index()
	return df[df['product_key'] >= product['product_key'].nunique()][['customer_id']]
```
